import { ComponentSize } from '../sushi.types';

/** Surface treatments available to cards. */
export type CardVariant = 'plain' | 'border' | 'dash';
/** Size scale controlling card content density. */
export type CardSize = ComponentSize;
