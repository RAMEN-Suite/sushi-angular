import { FormControlSeverity, FormControlSize } from '../form-control';

/** Size scale available to native inputs. */
export type InputSize = FormControlSize;
/** Semantic colors available to native inputs. */
export type InputSeverity = FormControlSeverity;
